#!/usr/bin/env bash
set -euo pipefail
SECRETS_FILE="\/home/hyperbots/.config/ai-global-tools/mcp-secrets.env"
if [ -f "$SECRETS_FILE" ]; then
  set -a
  # shellcheck disable=SC1090
  . "$SECRETS_FILE"
  set +a
fi
: "${FIRECRAWL_RETRY_MAX_ATTEMPTS:=5}"
: "${FIRECRAWL_RETRY_INITIAL_DELAY:=2000}"
: "${FIRECRAWL_RETRY_MAX_DELAY:=30000}"
: "${FIRECRAWL_RETRY_BACKOFF_FACTOR:=3}"
: "${FIRECRAWL_CREDIT_WARNING_THRESHOLD:=2000}"
: "${FIRECRAWL_CREDIT_CRITICAL_THRESHOLD:=500}"
export FIRECRAWL_RETRY_MAX_ATTEMPTS FIRECRAWL_RETRY_INITIAL_DELAY FIRECRAWL_RETRY_MAX_DELAY FIRECRAWL_RETRY_BACKOFF_FACTOR FIRECRAWL_CREDIT_WARNING_THRESHOLD FIRECRAWL_CREDIT_CRITICAL_THRESHOLD
exec npx -y firecrawl-mcp
